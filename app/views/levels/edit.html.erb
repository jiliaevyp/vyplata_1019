<!DOCTYPE html>
<h2>Администрирование</h2>
<html>
<head>
  <title>Изменение прав доступа</title>
</head>
<body>
<div class="container">

  <div class="header">Начисление заработной платы<br> ООО "Геопласт Телеком" </div>

  <div class="sidebar">
    <% if $glob_permition >0 %>
      <%=render('welcome/form_glob_sidebar') %>
    <% end %>
    </div>

  <div class="content">
      <% if $glob_permition > 0 %>
        <%= render 'form_edit_level' %><br>
      <button class="button"><%= link_to 'Выход', level_path %></button>
    <% else %>
      <h1><%= link_to 'Вас нет доступа!', welcome_index_path %></h1>
    <% end %>
  </div>

</div>
</body>
</html>
